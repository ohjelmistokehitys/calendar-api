{
	"name": "calendar-api",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev --ip 0.0.0.0",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings",
		"test": "vitest run"
	},
	"dependencies": {
		"@ohjelmistokehitys/calendar-converter": "^1.0.2",
		"hono": "^4.9.9"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20251001.0",
		"vitest": "^3.2.4",
		"wrangler": "^4.4.0"
	}
}
